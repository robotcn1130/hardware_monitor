# 安全政策

## 报告安全问题

如果你发现了安全漏洞（例如程序会意外破坏数据、提权逻辑可被利用、驱动安装流程有问题），
请**不要**直接开公开 Issue，改用 GitHub 的私密报告入口：

> 仓库页面 → **Security** 标签 → **Report a vulnerability**

也可以直接给维护者发 GitHub 私信。我们会在 7 天内回复。

可以公开讨论的**一般性缺陷**（读数不准、界面错乱、崩溃）请正常开 Issue，并附上：

- 程序版本号（右键菜单底部能看到）
- Windows 版本与 Build 号
- `logs/error.log` 里对应的片段

## 关于"杀毒软件报警"

少数杀毒软件会对打包后的 exe 报警。原因是 PyInstaller 这类打包工具会把全部依赖塞进
一个自解压程序，**行为特征上与某些恶意软件相似**，属于典型的**误报**。

如果你不放心，可以把 `硬件监控.zip` 上传到 [VirusTotal](https://www.virustotal.com/)
先扫一遍再解压。如果有引擎报毒且你认为是真的问题，请按上面的私密渠道告诉我们。

## 本程序会做什么、不会做什么

**会做**：

- 读取本机硬件传感器（CPU 寄存器、主板 SMBus、显卡 NVML 接口）
- 通过 ETW 事件追踪统计前台游戏的帧率
- 首次运行时静默安装随包分发的 PawnIO 内核驱动（读 CPU 温度必需）
- 在 exe 同目录读写 `config.json`（窗口位置、显示项、告警阈值）
- 在 `logs/error.log` 写错误日志（512 KB 轮转）

**不会做**：

- **不联网**。没有任何出站请求，不上传任何数据，也没有更新检查
- **不注入任何进程**，不挂钩 Direct3D / 交换链，不读写他人进程内存
- 不安装除 PawnIO 以外的任何驱动或服务（开机自启用的是 Windows 计划任务）
- 不收集硬件序列号、用户名、机器码等可以标识个人的信息

源码完全公开，欢迎自行审阅。
